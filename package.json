{
  "name": "esy-meson",
  "version": "0.50.000",
  "description": "Meson (build system) packaged for esy",
  "esy": {
    "buildsInSource": true,
    "exportedEnv": {
      "PATH": {
        "scope": "global",
        "val": "#{self.bin}:$PATH"
      },
      "PYTHONPATH": {
        "scope": "global",
        "val": "#{self.install / 'lib' / 'python3.7' / 'site-packages'}:$PYTHONPATH"
      }
    },
    "buildEnv": {
      "PYTHONPATH": "#{self.install / 'lib' / 'python3.7' / 'site-packages' }:$PYTHONPATH"
    },
    "install": [
      "mkdir -p #{self.install / 'lib' / 'python3.7' / 'site-packages' }",
      "python3 setup.py install --prefix #{self.install}"
    ]
  },
  "dependencies": {
    "esy-python": "esy-packages/esy-python#22a58395ae687852fee64ff6e752bd1354a4f343"
  }
}
